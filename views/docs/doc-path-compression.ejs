<%- include('../components/page-top', { title: 'Somente Path Compression', bodyClass: 'doc-body', stylesheets: ['/css/docs.css'], wrapperOpen: '<div class=\"container py-4 with-sidebar doc-page\">' }) %>
            <div class="mb-3">
                <a href="/documentacao" class="text-decoration-none"><i class="fa-solid fa-arrow-left"></i>
                    Voltar para Documentação</a>
            </div>

            <div class="doc-wrap">
                <article class="doc-article">

                    <h1>Somente Path Compression</h1>

                    <p>Neste modo, a DSU mantém a operação <code>union(x, y)</code> <strong>ingênua</strong> (sem heurística de tamanho ou rank),
                        mas a operação <code>find(x)</code> aplica <strong>Path Compression</strong>.</p>

                    <h2>0. Resumo</h2>

                    <ul>
                        <li><strong>O que otimiza:</strong> apenas a operação <code>find</code>; a união continua sem heurística de size ou rank.</li>
                        <li><strong>Ideia:</strong> sempre que <code>find</code> é executado, os nós visitados passam a apontar direto para a raiz.</li>
                        <li><strong>No simulador:</strong> essa compressão pode aparecer tanto durante <code>union</code> quanto no botão <code>Buscar (find)</code>.</li>
                        <li><strong>Efeito visual:</strong> a árvore vai ficando mais "achatada" ao longo das uniões e das buscas.</li>
                    </ul>

                    <h2>1. O que muda em relação ao modo ingênuo?</h2>

                    <p>Nesta sequência específica do simulador, as primeiras operações de <code>union</code> quase não alteram
                        visualmente a estrutura da árvore. A diferença torna-se mais evidente na última união e também quando
                        se executa <code>find</code> manualmente:</p>

                    <ul>
                        <li>No modo ingênuo, <code>find(x)</code> apenas sobe pelos pais até encontrar a raiz.</li>
                        <li>No modo com Path Compression, além de encontrar a raiz, <code>find(x)</code> atualiza os nós do caminho para apontarem
                            direto para a raiz.</li>
                    </ul>

                    <p>Na prática, isso <strong>achata</strong> a árvore ao longo das operações: depois de alguns <code>find</code>, muitos nós passam a ficar
                        a 1 aresta da raiz.</p>

                    <h2>2. Implementação típica</h2>

                    <p>A implementação é a mesma ideia usada nos modos otimizados, mas aplicada sobre uma DSU básica:</p>

                    <pre><code>
find(x):
  se parent[x] != x:
    parent[x] = find(parent[x])
  retorna parent[x]
</code></pre>

                    <p>No simulador, a operação <code>union</code> continua simples, sem heurística de size/rank. Ainda assim, como
                        <code>union</code> chama <code>find</code> internamente, pode haver compressão de caminho durante as uniões.</p>

                    <h2>3. Algoritmos (pseudocódigo)</h2>

                    <pre><code>
make_set(x):
  parent[x] = x

find(x):  // com path compression
  se parent[x] != x:
    parent[x] = find(parent[x])
  retorna parent[x]

union(x, y):  // ingênuo (sem size/rank)
  rx = find(x)
  ry = find(y)
  se rx != ry:
    parent[ry] = rx
</code></pre>

                    <h2>4. Complexidade (intuição)</h2>

                    <p>No modo ingênuo, o <code>find</code> pode percorrer uma cadeia grande (árvore alta), custando até <code>O(n)</code>.</p>
                    <p>Com Path Compression, cada <code>find</code> tende a achatar caminhos, então buscas futuras ficam mais baratas.
                        Por isso, a análise mais comum é <strong>amortizada</strong> (ao longo de várias operações), e é comum encontrar
                        o resultado <code>O(log n)</code> amortizado para “apenas Path Compression”.</p>

                    <h2>5. Como ver no simulador</h2>

                    <p>Selecione o modo <strong>Somente Path Compression</strong> e use <code>Buscar (find)</code> para observar, passo a passo,
                        as atualizações do vetor <code>parent</code> causadas pela compressão de caminho. Esse botão é útil porque isola
                        visualmente o efeito do <code>find</code>, mesmo que algumas compressões também possam ocorrer durante <code>union</code>.</p>

                    <h2>6. O que observar no simulador</h2>

                    <ul>
                        <li>Faça algumas uniões primeiro para formar uma árvore mais alta; parte dos caminhos pode até ser encurtada durante essas uniões.</li>
                        <li>Em seguida, execute <code>find</code> em um nó mais profundo e observe o vetor <code>parent</code> sendo atualizado.</li>
                        <li>Repita o mesmo <code>find</code>: o caminho deve ficar menor (muitas vezes vira 1 aresta até a raiz).</li>
                        <li>O painel de <strong>Pseudocódigo</strong> antecipa a <strong>próxima</strong> <code>union</code> antes do clique em <code>Próximo Passo</code>, o que ajuda a acompanhar onde a compressão pode ocorrer.</li>
                    </ul>

                    <h2>7. Quando a compressão aparece automaticamente nas unions?</h2>

                    <p>Nesta sequência, o <strong>Path Compression</strong> existe desde o início, mas ele só produz mudança visual
                        quando o <code>find</code> percorre um caminho com <strong>2 ou mais saltos</strong> até a raiz.</p>

                    <p>Nas primeiras uniões, os nós usados como argumentos ainda são raízes ou estão a apenas 1 salto da raiz.
                        Nesses casos, o <code>find</code> até é executado, mas comprimir o caminho significa manter o mesmo pai,
                        ou seja, não há mudança visível.</p>

                    <p>A diferença aparece no último passo, <code>union(h, d)</code>. Nesse momento, <code>d</code> já está em um
                        caminho mais profundo até a raiz, então o <code>find(d)</code> efetivamente encurta esse caminho antes da
                        união final entre os componentes.</p>

                    <p>Há um detalhe importante: nessa etapa, o <code>find(d)</code> ainda enxerga <code>e</code> como raiz do seu
                        componente. Por isso, a compressão faz <code>c → e</code> e <code>d → e</code>. Só depois dessa compressão
                        a <code>union</code> conecta as raízes dos dois componentes, fazendo <code>e → g</code>. Assim, os nós
                        comprimidos passam primeiro a apontar para <code>e</code>, e não diretamente para <code>g</code>.</p>

                    <p>Trace completo:</p>

                    <table>
                        <thead>
                            <tr>
                                <th>Step</th>
                                <th>Union</th>
                                <th>Nós usados</th>
                                <th>Saltos</th>
                                <th>Compressão durante union?</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>1</td>
                                <td><code>union(a, b)</code></td>
                                <td><code>a</code> (raiz), <code>b</code> (raiz)</td>
                                <td>0 saltos</td>
                                <td>não muda</td>
                            </tr>
                            <tr>
                                <td>2</td>
                                <td><code>union(c, d)</code></td>
                                <td><code>c</code> (raiz), <code>d</code> (raiz)</td>
                                <td>0 saltos</td>
                                <td>não muda</td>
                            </tr>
                            <tr>
                                <td>3</td>
                                <td><code>union(e, f)</code></td>
                                <td><code>e</code> (raiz), <code>f</code> (raiz)</td>
                                <td>0 saltos</td>
                                <td>não muda</td>
                            </tr>
                            <tr>
                                <td>4</td>
                                <td><code>union(g, h)</code></td>
                                <td><code>g</code> (raiz), <code>h</code> (raiz)</td>
                                <td>0 saltos</td>
                                <td>não muda</td>
                            </tr>
                            <tr>
                                <td>5</td>
                                <td><code>union(b, d)</code></td>
                                <td><code>b → a</code>, <code>d → c</code></td>
                                <td>1 salto cada</td>
                                <td>já aponta para a raiz</td>
                            </tr>
                            <tr>
                                <td>6</td>
                                <td><code>union(f, b)</code></td>
                                <td><code>f → e</code>, <code>b → a</code></td>
                                <td>1 salto cada</td>
                                <td>já aponta para a raiz</td>
                            </tr>
                            <tr>
                                <td>7</td>
                                <td><code>union(h, d)</code></td>
                                <td><code>h → g</code>, <code>d → c → a → e</code></td>
                                <td>1 salto e 3 saltos</td>
                                <td>sim: <code>c → e</code> e <code>d → e</code></td>
                            </tr>
                        </tbody>
                    </table>

                    <p>Depois do passo 6, o nó <code>d</code> passa a ter o caminho <code>d → c → a → e</code>. Como ele é usado
                        diretamente no passo 7, a compressão aparece de forma automática dentro da própria <code>union</code>.</p>

                    <h2>8. Onde o efeito visual fica claro</h2>

                    <p>Após as 7 unions, a árvore já aparece parcialmente achatada por causa da compressão ocorrida em
                        <code>union(h, d)</code>:</p>

                    <pre><code>
g
├── h
└── e
    ├── f
    ├── a
    │   └── b
    ├── c
    └── d
</code></pre>

                    <p>Nesse estado, ainda há nós que podem ser comprimidos mais uma vez. Ao clicar em <code>Buscar</code> para o nó
                        <code>b</code>, por exemplo, o simulador consegue mostrar claramente uma nova compressão:</p>

                    <p>Antes: <code>b → a → e → g</code></p>
                    <p>Depois: <code>b → g</code>, com os nós intermediários visitados passando a apontar diretamente para a raiz.</p>

                    <p>Esse é o principal valor didático do botão <code>Buscar</code> no modo <strong>Somente Path Compression</strong>:
                        mostrar que a árvore pode continuar sendo achatada mesmo depois de uma compressão automática ocorrida
                        dentro de <code>union</code>.</p>

                </article>
            </div>
<%- include('../components/page-bottom', { wrapperClose: '</div>' }) %>
